const canvas = document.querySelector("canvas");
const c = canvas.getContext("2d");

let stars = [];
let miniStars = [];
let backgroundStars = [];

module.exports = { canvas, c, stars, miniStars, backgroundStars };
